
@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

@using (Html.BeginForm())
{
    <div class="form-group">
        @Html.Label("xAxis")
        @Html.TextBox("xAxis",null,new { @class="form-control"})
    </div>
    <div class="form-group">
        @Html.Label("yAxis")
        @Html.TextBox("yAxis", null, new { @class = "form-control" })
    </div>

    <input type="submit" value="lav chart" class="btn btn-primary" />
}

